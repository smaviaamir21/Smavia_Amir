#include<iostream>
using namespace std;
int main() {
    float length, width;
    cout << "Enter length and width of the rectangle: ";
    cin >> length >> width;
    cout << "Area of rectangle is: " << length * width << endl;
    return 0;
}
